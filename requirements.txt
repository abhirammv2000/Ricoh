# Citera dependencies
# Locked to stable versions tested on Python 3.11
streamlit==1.42.0

PyMuPDF==1.25.3
chromadb==0.6.3
rank_bm25==0.2.2
langgraph==0.2.74
langchain-core>=0.3.34
langchain-anthropic==0.3.12
python-dotenv==1.0.1
# numpy is a transitive runtime dependency of rank_bm25 (BM25Okapi);
# pinned here for reproducible builds.
numpy==2.2.2

# API backend: FastAPI app plus the ASGI server that runs it. chromadb happens
# to pull these in too, but the API depends on them directly, so they are
# declared here rather than relied on as a transitive dependency.
fastapi==0.141.1
uvicorn[standard]==0.52.1
