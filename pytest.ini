[pytest]
# Put the project root on sys.path so `import src...` and
# `from tests.helpers import ...` both resolve without installing
# the package. Keeps `pytest` runnable straight from a clean clone.
pythonpath = .
testpaths = tests
addopts = -q
